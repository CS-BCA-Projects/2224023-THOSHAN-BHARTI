<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Profile</title>
  <link rel="stylesheet" href="/styles/style.css" />
  <style>
    body {
      background-color: #000;
      color: #fff;
      font-family: 'Segoe UI', sans-serif;
    }

    .profile-container {
      padding: 2rem;
      max-width: 800px;
      margin: auto;
      text-align: center;
    }

    h1 {
      color: hotpink;
      margin-bottom: 1rem;
    }

    .user-info {
      margin-bottom: 2rem;
      border: 2px solid hotpink;
      border-radius: 12px;
      padding: 1rem;
      background-color: #111;
    }

    .user-info input {
      padding: 0.5rem;
      margin-top: 1rem;
      border-radius: 8px;
      border: 1px solid #444;
    }

    .track-list {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
      gap: 1rem;
    }

    .track {
      background-color: #111;
      padding: 1rem;
      border-radius: 12px;
      border: 1px solid hotpink;
      transition: 0.3s;
    }

    .track:hover {
      transform: scale(1.05);
      background-color: #222;
    }

    .track audio {
      width: 100%;
      margin-top: 0.5rem;
    }

    .logout-btn, .remove-btn, .edit-btn {
      background: hotpink;
      color: black;
      padding: 0.5rem 1rem;
      margin: 1rem;
      border-radius: 12px;
      cursor: pointer;
      text-decoration: none;
      border: none;
      transition: 0.3s;
    }

    .remove-btn {
      background: crimson;
      color: white;
      margin-top: 1rem;
    }

    .remove-btn:hover, .logout-btn:hover, .edit-btn:hover {
      background-color: deeppink;
    }
  </style>
</head>
<body>
  <div class="profile-container">
    <h1>Your Profile</h1>

    <div class="user-info">
      <form action="/update-profile" method="POST">
        <p><strong>Username:</strong> <input type="text" name="username" value="<%= user.username %>" required /></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <button type="submit" class="edit-btn">Save</button>
      </form>
    </div>

    <a href="/logout" class="logout-btn">Logout</a>

    <h2 style="color: hotpink;">Your Favorites</h2>
    <div class="track-list">
      <% if (favorites.length > 0) { %>
        <% favorites.forEach(track => { %>
          <div class="track">
            <p><%= track.name %></p>
            <audio controls>
              <source src="/tracks/<%= track.filename %>" type="audio/mpeg" />
            </audio>
            <form action="/remove-favorite" method="POST">
              <input type="hidden" name="trackName" value="<%= track.filename %>" />
              <button type="submit" class="remove-btn">Remove</button>
            </form>
          </div>
        <% }) %>
      <% } else { %>
        <p>No favorites yet!</p>
      <% } %>
    </div>
  </div>
</body>
</html>
